/* The angle value α in radians (0 ≤ α < 2·π) is given. Convert this value into degrees. 
Take into account that 180° = π radians. Use 3.14 for a value of π. */


function convert(π) {
    var α = π / 3
    return α;
}

var case1 = 60;
var case1 = 3.14;
var result1 = convert(case1, case2);
console.log(result1);