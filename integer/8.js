/* A three-digit integer is given. Using one operator of integer division 
find first digit of the given integer (a hundreds digit).*/

function ammount(a, b) {
    var c = a / b;
    return c;
}

var firstTermen = 1;
var secondTermen = 13;
var result1 = ammount(firstTermen, secondTermen);
console.log(result1);